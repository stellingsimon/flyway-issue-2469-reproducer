Example reproducing Flyway Issue #2469
======================================

flyway clean
flyway migrate

run against a Postgres database with default usernames/passwords on default port, e.g. with

$ docker run -p 5432:5432 postgres:11
